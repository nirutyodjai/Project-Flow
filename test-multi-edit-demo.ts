/**
 * ไฟล์ทดสอบ Multi-Edit
 * มีโค้ดซ้ำกัน 10 อัน
 */

// Function 1
export function calculateNewPrice(price: number): number {
  const newTax = 0.07;
  const newDiscount = 0.05;
  return price * (1 + newTax) * (1 - newDiscount);
}

// Function 2
export function calculateNewPrice2(price: number): number {
  const newTax = 0.07;
  const newDiscount = 0.05;
  return price * (1 + newTax) * (1 - newDiscount);
}

// Function 3
export function calculateNewPrice3(price: number): number {
  const newTax = 0.07;
  const newDiscount = 0.05;
  return price * (1 + newTax) * (1 - newDiscount);
}

// Function 4
export function calculateNewPrice4(price: number): number {
  const newTax = 0.07;
  const newDiscount = 0.05;
  return price * (1 + newTax) * (1 - newDiscount);
}

// Function 5
export function calculateNewPrice5(price: number): number {
  const newTax = 0.07;
  const newDiscount = 0.05;
  return price * (1 + newTax) * (1 - newDiscount);
}

// Function 6
export function calculateNewPrice6(price: number): number {
  const newTax = 0.07;
  const newDiscount = 0.05;
  return price * (1 + newTax) * (1 - newDiscount);
}

// Function 7
export function calculateNewPrice7(price: number): number {
  const newTax = 0.07;
  const newDiscount = 0.05;
  return price * (1 + newTax) * (1 - newDiscount);
}

// Function 8
export function calculateNewPrice8(price: number): number {
  const newTax = 0.07;
  const newDiscount = 0.05;
  return price * (1 + newTax) * (1 - newDiscount);
}

// Function 9
export function calculateNewPrice9(price: number): number {
  const newTax = 0.07;
  const newDiscount = 0.05;
  return price * (1 + newTax) * (1 - newDiscount);
}

// Function 10
export function calculateNewPrice10(price: number): number {
  const newTax = 0.07;
  const newDiscount = 0.05;
  return price * (1 + newTax) * (1 - newDiscount);
}

// Constants
const NEW_API_URL = 'https://new-api.example.com';
const NEW_API_KEY = 'new-key-67890';
const NEW_TIMEOUT = 10000;
const NEW_RETRY = 3;
const NEW_VERSION = '1.0.0';
const NEW_STATUS = 'active';
const NEW_MODE = 'modern';
const NEW_TYPE = 'new-type';
const NEW_FORMAT = 'new-format';
const NEW_ENCODING = 'old-encoding';

console.log('Using NEW_API_URL:', NEW_API_URL);
console.log('Using NEW_API_KEY:', NEW_API_KEY);
console.log('Using NEW_TIMEOUT:', NEW_TIMEOUT);
